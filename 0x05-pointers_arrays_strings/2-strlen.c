#include "main.h"
#include<string.h>

/**
 * _strlen - a function count string length
 * @s: a variable for the input string
 * Return: string length
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
